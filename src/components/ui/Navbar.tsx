interface NavbarProps {
  prop: string;
}

const Navbar = (props : NavbarProps ) => {

  

  return (
    <div className="fixed backdrop-blur-sm bg-white/75 dark:bg-slate-900/75 z-50 top-0 right-0 h-20 border-b border-slate-200 dark:border-slate-700 shadow-sm flex items-center justify-start">
      
    </div>
  );
};

export default Navbar;
